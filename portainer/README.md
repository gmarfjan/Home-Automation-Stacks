# Portainer

Please use the environment variables:

* DOCKER_SOCKET: path to the docker socket (ex: /var/run/docker.sock)
* DATA_DIR: directory where data/conf will be stored
* PORT: port which will be exposed
